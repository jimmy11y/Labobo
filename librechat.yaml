version: 1.1.0
endpoints:
  custom:
    - name: "NVIDIA-NIM"
      apiKey: "${NVIDIA_API_KEY}"
      baseURL: "https://integrate.api.nvidia.com/v1"
      models:
        modelOptions:
          - "z-ai/glm-5.1"
          - "deepseek-ai/deepseek-v4-pro"
      vision: false
